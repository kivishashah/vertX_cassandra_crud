# vertX_cassandra_crud

Implement CRUD operations in Cassandra database with VertX.
Install Cassandra 
run cassandra script file(./cassandra)
create a keyspace, create a table in keyspace
you can run database operations in cqlsh.
$ cqlsh
